import SwiftUI

/// Stub: welcome animation was removed. Kept so existing build references resolve.
struct WelcomeAnimationOverlay: View {
    let theme: AppTheme
    let onDismiss: () -> Void

    var body: some View {
        EmptyView()
    }
}
